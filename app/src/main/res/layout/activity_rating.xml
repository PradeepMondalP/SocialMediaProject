<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".RatingActivity">

    <include
        layout="@layout/app_bar_layout"
        android:id="@+id/id_nav_bar_ratingActivity">

    </include>

    <RatingBar
        android:layout_width="wrap_content"
        android:stepSize="0.2"
        android:id="@+id/id_ratingBar"
        android:layout_centerInParent="true"
        android:layout_height="wrap_content" />


    <TextView
        android:id="@+id/id_rating_result"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:textSize="30sp"
        android:layout_marginBottom="40dp"
        android:textStyle="bold" />

    <Button
        android:layout_width="wrap_content"
        android:id="@+id/id_rating_button"
        android:layout_marginTop="30dp"
        android:layout_below="@+id/id_ratingBar"
        android:text="submit"
        android:textStyle="bold"
        android:textSize="27sp"
        android:layout_centerHorizontal="true"
        android:layout_height="wrap_content" />

    <TextView
        android:layout_width="match_parent"
        android:layout_marginTop="30dp"
        android:id="@+id/id_rating_display"
        android:textSize="25sp"
        android:layout_below="@+id/id_rating_button"
        android:layout_height="wrap_content" />


</RelativeLayout>